{
  "name": "@primate/monorepo",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "publish": "pnpm -r --access=public publish",
    "upgrade": "pnpm -r update -i -L",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "pnpm -r test"
  },
  "private": true,
  "devDependencies": {
    "@rcompat/test": "^0.2.0",
    "@rcompat/type": "^0.2.0",
    "apekit": "^0.0.16",
    "esbuild": "^0.25.5",
    "eslint": "^9.29.0",
    "proby": "^0.2.0",
    "typescript": "^5.8.3"
  }
}
