{
  "name": "@primate/monorepo",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "publish": "pnpm -r --access=public publish",
    "upgrade": "pnpm -r update -i -L",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npx proby"
  },
  "private": true,
  "devDependencies": {
    "@rcompat/test": "^0.1.16",
    "@rcompat/type": "^0.1.13",
    "apekit": "^0.0.16",
    "esbuild": "^0.25.4",
    "eslint": "^9.26.0",
    "proby": "^0.1.1",
    "typescript": "^5.8.3"
  }
}
